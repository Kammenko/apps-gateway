server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: https://apps-discovery-server.herokuapp.com/eureka

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes: